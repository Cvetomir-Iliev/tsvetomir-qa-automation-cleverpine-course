// basic assignment
let testResult;
testResult = "Pending";
console.log("Initial result:", testResult);

// Compound assignment operators
let errCount = 0;
errCount += 1;
console.log("Errors so far: ", errCount);

//Building test messages incrementally
let testlLog = " - Test started.";
testlLog += " - Navigation completed.";
testlLog += " - Login Attempt.";
testlLog += " - Verification passed.";
console.log("Final Log:", testlLog);
